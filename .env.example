# Local server
PORT=4321

# Optional language model synthesis for POST /api/ask.
# Without these, /api/ask still returns deterministic graph-backed answers.
LLM_API_KEY=
LLM_MODEL=
LLM_BASE_URL=https://api.openai.com/v1
